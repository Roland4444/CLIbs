
WHATS DONE:
    simple summ;
    simple summ w Pointers;
    wrapped simple summ;
    wrapped w Pointers;
    summ with struct;
    summ with Pointers to struct;
    wraoed summ to pointers to struct;

Roadmap:

Everything complete;
Need to call it from Scala;
